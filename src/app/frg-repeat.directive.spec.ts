import { FrgRepeatDirective } from './frg-repeat.directive';

describe('FrgRepeatDirective', () => {
  it('should create an instance', () => {
    const directive = new FrgRepeatDirective();
    expect(directive).toBeTruthy();
  });
});
